{
  "name": "bookworld",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-09-25",
    "deploy": "wrangler pages deploy public --project-name=bookworld"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.139.0"
  }
}
